export default class User {
  constructor(username, email, firstName, lastName, role, password) {
    this.username = username
    this.email = email
    this.firstName = firstName
    this.lastName = lastName
    this.role = role
    this.password = password
  }
}
